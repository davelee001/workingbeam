{
  "name": "working-beam-server",
  "version": "1.0.0",
  "description": "Authentication, escrow, Beam wallet, and notification API for WorkingBeam",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "npm run build && node --watch dist/index.js",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemailer": "^9.0.3",
    "numeric": "^1.2.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "@types/nodemailer": "^8.0.1",
    "@types/numeric": "^1.2.2",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
